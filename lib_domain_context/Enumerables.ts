export enum Types
{
	Persons = 0,
	PersonTypes = 1,
};

export enum Architecture {
	StandAlone = 0,
	Services = 1,
};

export enum Message {
	MESSAGE = 0,
	QUESTION = 1,
};

export enum Loading {
	ADD = 0,
	REMOVE = 1,
};

export enum Action {
	OPEN = 0,
	CLOSE = 1,
};